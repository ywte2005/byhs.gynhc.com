<template>
	<view class="page">
		<!-- 顶部导航栏 -->
		<view class="navbar">
			<view class="navbar-inner">
				<view class="navbar-left"></view>
				<view class="navbar-center"></view>
				<view class="navbar-right">
					<view class="top-icon" @tap="toSet">
						<text class="icon-text">⚙️</text>
					</view>
					<view class="top-icon" @tap="toMessage">
						<text class="icon-text">🔔</text>
					</view>
				</view>
			</view>
		</view>

		<!-- 内容区域 -->
		<scroll-view class="content" scroll-y>
			<!-- 用户信息 -->
			<view class="user-section" @tap="toEdit">
				<view class="user-avatar-wrapper">
					<image
						class="user-avatar"
						:src="avatarUrl || '/static/default-avatar.png'"
						mode="aspectFill"
					/>
					<view v-if="isLogin" class="avatar-edit-badge">
						<text class="badge-text">✏️</text>
					</view>
				</view>
				<view class="user-info">
					<text class="user-name">{{ nickName || '未登录' }}</text>
					<text v-if="isLogin" class="user-phone">{{ phone }}</text>
				</view>
			</view>

			<!-- 数据统计 -->
			<view class="stats-section">
				<view class="stat-item">
					<text class="stat-value">171</text>
					<text class="stat-label">总点击</text>
				</view>
				<view class="stat-item">
					<text class="stat-value">24</text>
					<text class="stat-label">赞</text>
				</view>
				<view class="stat-item">
					<text class="stat-value">89</text>
					<text class="stat-label">收藏</text>
				</view>
				<view class="stat-item">
					<text class="stat-value">653</text>
					<text class="stat-label">粉丝</text>
				</view>
			</view>

			<!-- 快捷开关 -->
			<view class="switch-section">
				<view class="switch-card">
					<view class="switch-info">
						<text class="switch-title">接单模式</text>
						<text class="switch-desc">已关闭</text>
					</view>
					<switch :checked="receiveOrderMode" @change="onReceiveOrderChange" />
				</view>
				<view class="switch-card">
					<view class="switch-info">
						<text class="switch-title">消息通知</text>
						<text class="switch-desc">已关闭</text>
					</view>
					<switch :checked="notificationMode" @change="onNotificationChange" />
				</view>
			</view>

			<!-- 订单状态 -->
			<view class="order-section">
				<view class="order-item" @tap="toTest">
					<text class="order-icon">💰</text>
					<text class="order-label">待支付</text>
				</view>
				<view class="order-item" @tap="toTest">
					<text class="order-icon">📦</text>
					<text class="order-label">未发货</text>
				</view>
				<view class="order-item" @tap="toTest">
					<view class="order-badge-wrapper">
						<text class="order-icon">✈️</text>
						<view class="order-badge">
							<text class="badge-num">3</text>
						</view>
					</view>
					<text class="order-label">已发货</text>
				</view>
				<view class="order-item" @tap="toTest">
					<text class="order-icon">💱</text>
					<text class="order-label">售后/退款</text>
				</view>
			</view>

			<!-- 功能列表 -->
			<view class="list-section">
				<view class="list-item" hover-class="list-item-hover" @tap="toWallet">
					<view class="list-left">
						<text class="list-icon">👛</text>
						<text class="list-label">我的钱包</text>
					</view>
					<text class="arrow">›</text>
				</view>
				<view class="list-item" hover-class="list-item-hover" @tap="toTest">
					<view class="list-left">
						<text class="list-icon">📊</text>
						<text class="list-label">数据看板</text>
					</view>
					<text class="arrow">›</text>
				</view>
				<view class="list-item" hover-class="list-item-hover" @tap="toTest">
					<view class="list-left">
						<text class="list-icon">🕐</text>
						<text class="list-label">历史记录</text>
					</view>
					<text class="arrow">›</text>
				</view>
				<view class="list-item" hover-class="list-item-hover" @tap="toTest">
					<view class="list-left">
						<text class="list-icon">📤</text>
						<text class="list-label">邀请好友</text>
					</view>
					<text class="arrow">›</text>
				</view>
			</view>

			<!-- 设置 -->
			<view class="list-section">
				<view class="list-item" hover-class="list-item-hover" @tap="toSet">
					<view class="list-left">
						<text class="list-icon">⚙️</text>
						<text class="list-label">设置</text>
					</view>
					<text class="arrow">›</text>
				</view>
			</view>
		</scroll-view>

		<!-- 底部导航栏 -->
		<view class="tabbar">
			<view class="tabbar-item" @tap="toHome">
				<text class="tabbar-icon">🏠</text>
				<text class="tabbar-text">首页</text>
			</view>
			<view class="tabbar-item" @tap="toTemplate">
				<text class="tabbar-icon">📋</text>
				<text class="tabbar-text">模板</text>
			</view>
			<view class="tabbar-item active">
				<text class="tabbar-icon">👤</text>
				<text class="tabbar-text active">我的</text>
			</view>
		</view>
	</view>
</template>

<script setup lang="uts">
import { ref } from 'vue'

// 用户信息
const isLogin = ref(true)
const avatarUrl = ref<string | null>(null)
const nickName = ref('张三')
const phone = ref('138****8888')

// 开关状态
const receiveOrderMode = ref(false)
const notificationMode = ref(false)

// 接单模式改变
function onReceiveOrderChange(e: any) {
	receiveOrderMode.value = e.detail.value
	uni.showToast({
		title: receiveOrderMode.value ? '接单模式已开启' : '接单模式已关闭',
		icon: 'none'
	})
}

// 消息通知改变
function onNotificationChange(e: any) {
	notificationMode.value = e.detail.value
	uni.showToast({
		title: notificationMode.value ? '消息通知已开启' : '消息通知已关闭',
		icon: 'none'
	})
}

// 跳转到编辑资料
function toEdit() {
	if (!isLogin.value) {
		uni.navigateTo({
			url: '/pages/user/login'
		})
		return
	}
	uni.navigateTo({
		url: '/pages/user/edit-simple'
	})
}

// 跳转到设置
function toSet() {
	uni.navigateTo({
		url: '/pages/set/index-simple'
	})
}

// 跳转到消息
function toMessage() {
	uni.navigateTo({
		url: '/pages/message/index'
	})
}

// 跳转到钱包
function toWallet() {
	uni.navigateTo({
		url: '/pages/wallet/index'
	})
}

// 跳转到首页
function toHome() {
	uni.switchTab({
		url: '/pages/index/home'
	})
}

// 跳转到模板
function toTemplate() {
	uni.switchTab({
		url: '/pages/index/template'
	})
}

// 测试功能
function toTest() {
	uni.showToast({
		title: '开发中，敬请期待',
		icon: 'none'
	})
}
</script>

<style scoped>
.page {
	width: 750rpx;
	min-height: 100vh;
	background-color: #f5f5f5;
	display: flex;
	flex-direction: column;
}

/* 导航栏 */
.navbar {
	background-color: transparent;
}

.navbar-inner {
	height: 88rpx;
	display: flex;
	flex-direction: row;
	align-items: center;
	padding: 0 30rpx;
}

.navbar-left,
.navbar-center {
	flex: 1;
}

.navbar-right {
	display: flex;
	flex-direction: row;
	align-items: center;
}

.top-icon {
	width: 64rpx;
	height: 64rpx;
	background-color: #ffffff;
	border-radius: 16rpx;
	display: flex;
	justify-content: center;
	align-items: center;
	margin-left: 20rpx;
}

.icon-text {
	font-size: 32rpx;
}

/* 内容区域 */
.content {
	flex: 1;
	padding: 30rpx;
	padding-bottom: 120rpx;
}

/* 用户信息 */
.user-section {
	display: flex;
	flex-direction: column;
	align-items: center;
	padding: 40rpx 0;
}

.user-avatar-wrapper {
	position: relative;
	width: 160rpx;
	height: 160rpx;
	margin-bottom: 30rpx;
}

.user-avatar {
	width: 160rpx;
	height: 160rpx;
	border-radius: 40rpx;
}

.avatar-edit-badge {
	position: absolute;
	bottom: 0;
	right: -6rpx;
	width: 48rpx;
	height: 48rpx;
	background-color: #000000;
	border-radius: 24rpx;
	border: 2px solid #ffffff;
	display: flex;
	justify-content: center;
	align-items: center;
}

.badge-text {
	font-size: 24rpx;
}

.user-info {
	display: flex;
	flex-direction: column;
	align-items: center;
}

.user-name {
	font-size: 36rpx;
	font-weight: 500;
	color: #333333;
	margin-bottom: 10rpx;
}

.user-phone {
	font-size: 24rpx;
	color: #999999;
}

/* 数据统计 */
.stats-section {
	display: flex;
	flex-direction: row;
	justify-content: space-around;
	padding: 40rpx 0;
	border-bottom: 1px solid #e5e5e5;
	margin-bottom: 30rpx;
}

.stat-item {
	display: flex;
	flex-direction: column;
	align-items: center;
}

.stat-value {
	font-size: 36rpx;
	font-weight: 500;
	color: #333333;
	margin-bottom: 10rpx;
}

.stat-label {
	font-size: 24rpx;
	color: #999999;
}

/* 快捷开关 */
.switch-section {
	display: flex;
	flex-direction: row;
	gap: 30rpx;
	margin-bottom: 30rpx;
}

.switch-card {
	flex: 1;
	background-color: #ffffff;
	border-radius: 16rpx;
	padding: 30rpx;
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: space-between;
}

.switch-info {
	display: flex;
	flex-direction: column;
	flex: 1;
	margin-right: 20rpx;
}

.switch-title {
	font-size: 28rpx;
	color: #333333;
	margin-bottom: 10rpx;
}

.switch-desc {
	font-size: 24rpx;
	color: #999999;
}

/* 订单状态 */
.order-section {
	background-color: #ffffff;
	border-radius: 16rpx;
	padding: 40rpx 0;
	margin-bottom: 30rpx;
	display: flex;
	flex-direction: row;
	justify-content: space-around;
}

.order-item {
	display: flex;
	flex-direction: column;
	align-items: center;
	padding: 0 20rpx;
}

.order-badge-wrapper {
	position: relative;
}

.order-icon {
	font-size: 48rpx;
	margin-bottom: 10rpx;
}

.order-badge {
	position: absolute;
	top: -10rpx;
	right: -10rpx;
	min-width: 32rpx;
	height: 32rpx;
	background-color: #ff3b30;
	border-radius: 16rpx;
	display: flex;
	justify-content: center;
	align-items: center;
	padding: 0 8rpx;
}

.badge-num {
	font-size: 20rpx;
	color: #ffffff;
}

.order-label {
	font-size: 24rpx;
	color: #666666;
	text-align: center;
}

/* 功能列表 */
.list-section {
	background-color: #ffffff;
	border-radius: 16rpx;
	margin-bottom: 30rpx;
	overflow: hidden;
}

.list-item {
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: space-between;
	padding: 30rpx;
	border-bottom: 1px solid #f5f5f5;
}

.list-item:last-child {
	border-bottom: none;
}

.list-item-hover {
	background-color: #f5f5f5;
}

.list-left {
	display: flex;
	flex-direction: row;
	align-items: center;
}

.list-icon {
	font-size: 40rpx;
	margin-right: 20rpx;
}

.list-label {
	font-size: 30rpx;
	color: #333333;
}

.arrow {
	font-size: 40rpx;
	color: #999999;
}

/* 底部导航栏 */
.tabbar {
	position: fixed;
	bottom: 0;
	left: 0;
	right: 0;
	height: 100rpx;
	background-color: #ffffff;
	border-top: 1px solid #e5e5e5;
	display: flex;
	flex-direction: row;
	padding-bottom: env(safe-area-inset-bottom);
}

.tabbar-item {
	flex: 1;
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
}

.tabbar-icon {
	font-size: 44rpx;
	margin-bottom: 8rpx;
}

.tabbar-text {
	font-size: 20rpx;
	color: #999999;
}

.tabbar-text.active {
	color: #007aff;
}
</style>
