<template>
	<view class="flex flex-col h-screen bg-[#f5f7fa]">
		<!-- Header Wrapper with Blue Background -->
		<view class="bg-[#0052d9] pb-[32rpx]">
			<!-- Topbar -->
			<cl-topbar :title="''" :show-back="false" background-color="transparent" color="#ffffff" safe-area-top :border="false">
				<template #append>
					<view class="flex flex-row gap-[24rpx] mr-[32rpx]">
						<view class="w-[64rpx] h-[64rpx] bg-white/20 rounded-[32rpx] flex items-center justify-center" @tap="toSet">
							<uni-icons type="settings" size="20" color="#ffffff"></uni-icons>
						</view>
						<view class="w-[64rpx] h-[64rpx] bg-white/20 rounded-[32rpx] flex items-center justify-center" @tap="toMessage">
							<uni-icons type="notification" size="20" color="#ffffff"></uni-icons>
						</view>
					</view>
				</template>
			</cl-topbar>

			<!-- User Info -->
			<view class="flex flex-row items-center gap-[24rpx] px-[32rpx] mt-[12rpx]" @tap="toEdit">
				<view class="w-[120rpx] h-[120rpx] rounded-[60rpx] bg-white border-[4rpx] border-white/30 flex items-center justify-center overflow-hidden relative">
					<image
						class="w-full h-full"
						:src="avatarUrl || '/static/default-avatar.png'"
						mode="aspectFill"
					/>
					<view v-if="isLogin" class="absolute bottom-0 right-0 w-full h-[30%] bg-black/50 flex items-center justify-center">
						<uni-icons type="compose" size="12" color="#ffffff"></uni-icons>
					</view>
				</view>
				<view class="flex-1 flex flex-col gap-[8rpx]">
					<text class="text-[36rpx] font-bold text-white">{{ nickName || '点击登录' }}</text>
					<text class="text-[24rpx] text-white/80" v-if="userInfo?.phone">ID: {{ userInfo?.phone }}</text>
				</view>
				<uni-icons type="right" size="16" color="rgba(255,255,255,0.6)"></uni-icons>
			</view>

			<!-- Stats -->
			<view class="flex flex-row justify-around mt-[32rpx]">
				<view class="flex flex-col items-center gap-[8rpx]">
					<text class="text-[36rpx] font-bold text-white">171</text>
					<text class="text-[24rpx] text-white/80">总点击</text>
				</view>
				<view class="flex flex-col items-center gap-[8rpx]">
					<text class="text-[36rpx] font-bold text-white">24</text>
					<text class="text-[24rpx] text-white/80">赞</text>
				</view>
				<view class="flex flex-col items-center gap-[8rpx]">
					<text class="text-[36rpx] font-bold text-white">89</text>
					<text class="text-[24rpx] text-white/80">收藏</text>
				</view>
				<view class="flex flex-col items-center gap-[8rpx]">
					<text class="text-[36rpx] font-bold text-white">653</text>
					<text class="text-[24rpx] text-white/80">粉丝</text>
				</view>
			</view>
		</view>

		<scroll-view class="flex-1 h-0 -mt-[24rpx] rounded-t-[24rpx] bg-[#f5f7fa] overflow-hidden" scroll-y>
			<view class="p-[32rpx] pb-[120rpx] flex flex-col gap-[24rpx]">
				<!-- 快捷开关 -->
				<view class="flex flex-row gap-[24rpx]">
					<view class="flex-1 flex flex-row items-center justify-between p-[24rpx] bg-white rounded-[24rpx]">
						<view class="flex flex-col gap-[4rpx]">
							<text class="text-[28rpx] font-bold text-[#1f2329]">接单模式</text>
							<text class="text-[22rpx] text-[#999999]">已关闭</text>
						</view>
						<switch :checked="receiveOrderMode" color="#0052d9" style="transform:scale(0.8)" @change="onReceiveOrderChange" />
					</view>
					<view class="flex-1 flex flex-row items-center justify-between p-[24rpx] bg-white rounded-[24rpx]">
						<view class="flex flex-col gap-[4rpx]">
							<text class="text-[28rpx] font-bold text-[#1f2329]">消息通知</text>
							<text class="text-[22rpx] text-[#999999]">已关闭</text>
						</view>
						<switch :checked="notificationMode" color="#0052d9" style="transform:scale(0.8)" @change="onNotificationChange" />
					</view>
				</view>

				<!-- 订单状态 -->
				<view class="bg-white rounded-[24rpx] py-[40rpx] px-[20rpx]">
					<view class="flex flex-row justify-around">
						<view class="flex flex-col items-center gap-[12rpx] relative" @tap="toTest">
							<uni-icons type="wallet" size="28" color="#0052d9"></uni-icons>
							<text class="text-[24rpx] text-[#646a73]">待支付</text>
						</view>
						<view class="flex flex-col items-center gap-[12rpx] relative" @tap="toTest">
							<uni-icons type="gift" size="28" color="#0052d9"></uni-icons>
							<text class="text-[24rpx] text-[#646a73]">未发货</text>
						</view>
						<view class="flex flex-col items-center gap-[12rpx] relative" @tap="toTest">
							<view class="relative">
								<uni-icons type="paperplane" size="28" color="#0052d9"></uni-icons>
								<view class="absolute -top-[10rpx] -right-[10rpx] min-w-[32rpx] h-[32rpx] bg-[#ff3b30] rounded-[16rpx] flex items-center justify-center border-[2rpx] border-white">
									<text class="text-[20rpx] text-white">3</text>
								</view>
							</view>
							<text class="text-[24rpx] text-[#646a73]">已发货</text>
						</view>
						<view class="flex flex-col items-center gap-[12rpx] relative" @tap="toTest">
							<uni-icons type="refresh" size="28" color="#0052d9"></uni-icons>
							<text class="text-[24rpx] text-[#646a73]">售后/退款</text>
						</view>
					</view>
				</view>

				<!-- 功能列表 -->
				<view class="bg-white rounded-[24rpx] px-[32rpx] flex flex-col">
					<view class="flex flex-row justify-between items-center h-[112rpx]" @tap="toWallet">
						<view class="flex flex-row items-center gap-[24rpx]">
							<uni-icons type="wallet" size="24" color="#0052d9"></uni-icons>
							<text class="text-[30rpx] text-[#1f2329]">我的钱包</text>
						</view>
						<uni-icons type="right" size="16" color="#c0c4cc"></uni-icons>
					</view>
					<view class="h-[2rpx] bg-[#f0f0f0]"></view>
					<view class="flex flex-row justify-between items-center h-[112rpx]" @tap="toTest">
						<view class="flex flex-row items-center gap-[24rpx]">
							<uni-icons type="bars" size="24" color="#0052d9"></uni-icons>
							<text class="text-[30rpx] text-[#1f2329]">数据看板</text>
						</view>
						<uni-icons type="right" size="16" color="#c0c4cc"></uni-icons>
					</view>
					<view class="h-[2rpx] bg-[#f0f0f0]"></view>
					<view class="flex flex-row justify-between items-center h-[112rpx]" @tap="toTest">
						<view class="flex flex-row items-center gap-[24rpx]">
							<uni-icons type="calendar" size="24" color="#0052d9"></uni-icons>
							<text class="text-[30rpx] text-[#1f2329]">历史记录</text>
						</view>
						<uni-icons type="right" size="16" color="#c0c4cc"></uni-icons>
					</view>
					<view class="h-[2rpx] bg-[#f0f0f0]"></view>
					<view class="flex flex-row justify-between items-center h-[112rpx]" @tap="toTest">
						<view class="flex flex-row items-center gap-[24rpx]">
							<uni-icons type="redo" size="24" color="#0052d9"></uni-icons>
							<text class="text-[30rpx] text-[#1f2329]">邀请好友</text>
						</view>
						<uni-icons type="right" size="16" color="#c0c4cc"></uni-icons>
					</view>
				</view>

				<!-- 设置 -->
				<view class="bg-white rounded-[24rpx] px-[32rpx] flex flex-col">
					<view class="flex flex-row justify-between items-center h-[112rpx]" @tap="toSet">
						<view class="flex flex-row items-center gap-[24rpx]">
							<uni-icons type="settings" size="24" color="#0052d9"></uni-icons>
							<text class="text-[30rpx] text-[#1f2329]">设置</text>
						</view>
						<uni-icons type="right" size="16" color="#c0c4cc"></uni-icons>
					</view>
				</view>
			</view>
		</scroll-view>

		<!-- 底部导航栏 -->
		<view class="fixed bottom-0 left-0 right-0 h-[100rpx] bg-white border-t border-[#e5e5e5] flex flex-row pb-[env(safe-area-inset-bottom)]">
			<view class="flex-1 flex flex-col items-center justify-center" @tap="toHome">
				<uni-icons type="home" size="24" color="#999999"></uni-icons>
				<text class="text-[20rpx] text-[#999999] mt-[4rpx]">首页</text>
			</view>
			<view class="flex-1 flex flex-col items-center justify-center" @tap="toTemplate">
				<uni-icons type="list" size="24" color="#999999"></uni-icons>
				<text class="text-[20rpx] text-[#999999] mt-[4rpx]">模板</text>
			</view>
			<view class="flex-1 flex flex-col items-center justify-center">
				<uni-icons type="person-filled" size="24" color="#0052d9"></uni-icons>
				<text class="text-[20rpx] text-[#0052d9] mt-[4rpx]">我的</text>
</template>

<script setup lang="uts">
import { router, useRefs, useStore, t } from "@/.cool";
import { useUi } from "@/uni_modules/cool-ui";
import ContactUs from "@/pages/set/components/contact-us.uvue";

const refs = useRefs();

// 用户信息
const isLogin = ref(true)
const avatarUrl = ref<string | null>(null)
const nickName = ref('张三')
const phone = ref('138****8888')

// 开关状态
const receiveOrderMode = ref(false)
const notificationMode = ref(false)

// 接单模式改变
function onReceiveOrderChange(e: any) {
	receiveOrderMode.value = e.detail.value
	uni.showToast({
		title: receiveOrderMode.value ? '接单模式已开启' : '接单模式已关闭',
		icon: 'none'
	})
}

// 消息通知改变
function onNotificationChange(e: any) {
	notificationMode.value = e.detail.value
	uni.showToast({
		title: notificationMode.value ? '消息通知已开启' : '消息通知已关闭',
		icon: 'none'
	})
}

// 跳转到编辑资料
function toEdit() {
	if (!user.token) {
		uni.navigateTo({
			url: '/pages/user/login'
		})
		return
	}
	uni.navigateTo({
		url: '/pages/user/edit'
	})
}

// 跳转到设置
function toSet() {
	uni.navigateTo({
		url: '/pages/set/index'
	})
}

// 跳转到消息
function toMessage() {
	uni.navigateTo({
		url: '/pages/message/index'
	})
}

// 跳转到钱包
function toWallet() {
	uni.navigateTo({
		url: '/pages/wallet/index'
	})
}

// 跳转到首页
function toHome() {
	uni.switchTab({
		url: '/pages/index/home'
	})
}

// 跳转到模板
function toTemplate() {
	uni.switchTab({
		url: '/pages/index/template'
	})
}

// 跳转到数据看板
function toPerformance() {
	uni.navigateTo({
		url: '/pages/promo/performance'
	})
}

// 测试功能
function toTest() {
	uni.showToast({
		title: '开发中，敬请期待',
		icon: 'none'
	})
}
</script>

<style>
.page-container {
	display: flex;
	flex-direction: column;
	background-color: #f5f7fa;
	height: 100vh;
}

/* Header */
.my-header {
	background-color: #0052d9;
	padding: 32rpx;
	padding-top: calc(20rpx + env(safe-area-inset-top));
	display: flex;
	flex-direction: column;
	gap: 32rpx;
}

.header-nav {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
}

.header-right {
	display: flex;
	flex-direction: row;
	gap: 24rpx;
}

.nav-icon {
	width: 64rpx;
	height: 64rpx;
	background-color: rgba(255, 255, 255, 0.2);
	border-radius: 32rpx;
	display: flex;
	align-items: center;
	justify-content: center;
}

.nav-icon-text {
	font-size: 32rpx;
}

.user-info {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 24rpx;
}

.avatar-wrapper {
	width: 120rpx;
	height: 120rpx;
	border-radius: 60rpx;
	background-color: #ffffff;
	border: 4rpx solid rgba(255, 255, 255, 0.3);
	overflow: hidden;
	display: flex;
	align-items: center;
	justify-content: center;
	position: relative;
}

.user-avatar {
	width: 100%;
	height: 100%;
}

.avatar-edit-badge {
	position: absolute;
	bottom: 0;
	right: 0;
	width: 100%;
	height: 30%;
	background-color: rgba(0, 0, 0, 0.5);
	display: flex;
	align-items: center;
	justify-content: center;
}

.badge-text {
	font-size: 20rpx;
}

.info-content {
	flex: 1;
	display: flex;
	flex-direction: column;
	gap: 8rpx;
}

.user-name {
	font-size: 36rpx;
	font-weight: 600;
	color: #ffffff;
}

.user-id {
	font-size: 24rpx;
	color: rgba(255, 255, 255, 0.8);
}

.arrow-right {
	font-size: 32rpx;
	color: rgba(255, 255, 255, 0.6);
}

.stats-row {
	display: flex;
	flex-direction: row;
	justify-content: space-around;
}

.stat-item {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 8rpx;
}

.stat-num {
	font-size: 36rpx;
	font-weight: 700;
	color: #ffffff;
}

.stat-label {
	font-size: 24rpx;
	color: rgba(255, 255, 255, 0.8);
}

/* Content */
.content {
	flex: 1;
	height: 0;
	margin-top: -24rpx;
	border-top-left-radius: 24rpx;
	border-top-right-radius: 24rpx;
	background-color: #f5f7fa;
	overflow: hidden;
}

.content-wrapper {
	padding: 32rpx;
	padding-bottom: 120rpx; /* Tabbar height */
	display: flex;
	flex-direction: column;
	gap: 24rpx;
}

.card {
	background-color: #ffffff;
	border-radius: 24rpx;
	padding: 32rpx;
	display: flex;
	flex-direction: column;
}

/* Switches */
.switch-row {
	display: flex;
	flex-direction: row;
	gap: 24rpx;
}

.switch-card {
	flex: 1;
	flex-direction: row;
	align-items: center;
	justify-content: space-between;
	padding: 24rpx;
}

.switch-info {
	display: flex;
	flex-direction: column;
	gap: 4rpx;
}

.switch-title {
	font-size: 28rpx;
	font-weight: 600;
	color: #1f2329;
}

.switch-desc {
	font-size: 22rpx;
	color: #999999;
}

/* Order Card */
.order-card {
	padding: 40rpx 20rpx;
}

.grid-row {
	display: flex;
	flex-direction: row;
	justify-content: space-around;
}

.grid-item {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 12rpx;
	position: relative;
}

.grid-icon {
	font-size: 48rpx;
}

.grid-label {
	font-size: 24rpx;
	color: #646a73;
}

.order-badge-wrapper {
	position: relative;
}

.order-badge {
	position: absolute;
	top: -10rpx;
	right: -10rpx;
	min-width: 32rpx;
	height: 32rpx;
	background-color: #ff3b30;
	border-radius: 16rpx;
	display: flex;
	align-items: center;
	justify-content: center;
	border: 2rpx solid #ffffff;
}

.badge-num {
	font-size: 20rpx;
	color: #ffffff;
}

/* Menu Card */
.menu-card {
	padding: 0 32rpx;
}

.menu-item {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	height: 112rpx;
}

.menu-left {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 24rpx;
}

.menu-icon {
	font-size: 40rpx;
}

.menu-label {
	font-size: 30rpx;
	color: #1f2329;
}

.menu-arrow {
	font-size: 30rpx;
	color: #c0c4cc;
}

.divider {
	height: 2rpx;
	background-color: #f0f0f0;
}

/* Tabbar */
.tabbar {
	position: fixed;
	bottom: 0;
	left: 0;
	right: 0;
	height: 100rpx;
	background-color: #ffffff;
	border-top: 1px solid #e5e5e5;
	display: flex;
	flex-direction: row;
	padding-bottom: env(safe-area-inset-bottom);
}

.tabbar-item {
	flex: 1;
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
}

.tabbar-icon {
	font-size: 44rpx;
	margin-bottom: 8rpx;
}

.tabbar-text {
	font-size: 20rpx;
	color: #999999;
}

.tabbar-text.active {
	color: #0052d9;
}
</style>
